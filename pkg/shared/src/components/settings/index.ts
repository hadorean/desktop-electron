/**
 * Settings UI components
 * 
 * Svelte components are available but not directly exported from TypeScript.
 * Import them directly using their file paths:
 * 
 * import ToggleControl from '@heyketsu/shared/src/components/settings/ToggleControl.svelte'
 */

// Component paths (for reference)
export const COMPONENT_PATHS = {
  ToggleControl: './ToggleControl.svelte'
} as const